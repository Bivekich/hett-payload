version: '3'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    # При необходимости можно добавить тома для хранения данных
    # volumes:
    #   - ./public:/app/public
